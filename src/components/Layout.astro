---
import BaseLayout, { Props as BaseProps, siteTitle } from "./BaseLayout.astro";
export { siteTitle };

export interface Props extends BaseProps {
}
const { metadescription, title } = Astro.props as Props;
---

<BaseLayout {title} {metadescription}>
    <slot />
</BaseLayout>
